name: Keep HF Space Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping HF Space
        run: |
          curl -s https://andysp07-n8npsyjournal.hf.space/ > /dev/null
